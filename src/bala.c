//#include "bala.h"
//#include "screen.h"

//#define WIDTH 80
//#define HEIGHT 24

//extern struct Nave nave;
//Bala balas[MAX_BALAS];

//void atirar() {
    //for (int i = 0; i < MAX_BALAS; i++) {
        //if (!balas[i].ativo) {
            //balas[i].x = nave.x + 2;
            //balas[i].y = HEIGHT - 3;
            //balas[i].ativo = 1;
            //break;
        //}
    //}
//}

//void moverTiros() {
    //for (int i = 0; i < MAX_BALAS; i++) {
        //if (balas[i].ativo) {
            //balas[i].y--;
            //if (balas[i].y < 0) {
                //balas[i].ativo = 0;
            //}
        //}
    //}
//}

#include "bala.h"
#include <stdio.h>

void print_bala() {
    printf("Função bala chamada!\n");
}

